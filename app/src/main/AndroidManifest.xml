<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.classroom.zed.classroom">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SigninActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ClassesActivity" />
        <activity android:name=".CreateClassActivity" />
        <activity android:name=".JoinClassActivity" />
        <activity android:name=".ClassPageActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".ClassInfoActivity" />
        <activity android:name=".ClassSettingsActivity" />
        <activity android:name=".CreateClassworkActivity" />
        <activity android:name=".ClassworkInfoActivity" />
        <activity android:name=".AddPeopleActivity" />
        <activity android:name=".ProfileActivity"></activity>
    </application>

</manifest>